// Generated by CoffeeScript 1.6.3
$(document).ready(function() {
  var AppRouter, LoginView, RegisterView, appRouter;
  LoginView = Backbone.View.extend({
    initialize: function() {
      return this.render();
    },
    render: function() {
      var template;
      template = _.template($("#login-template").html(), {});
      return this.$el.html(template);
    }
  });
  RegisterView = Backbone.View.extend({
    initialize: function() {
      return this.render();
    },
    render: function() {
      var template;
      template = _.template($("#register-template").html(), {});
      return this.$el.html(template);
    },
    events: {
      "click #register-btn": "doRegister"
    },
    doRegister: function() {}
  });
  AppRouter = Backbone.Router.extend({
    routes: {
      "": "index",
      "index": "index",
      "about": "about",
      "login": "login",
      "register": "register"
    }
  });
  appRouter = new AppRouter;
  appRouter.on("route:index", function() {
    return $(".login-container").hide();
  });
  appRouter.on("route:about", function() {
    $(".login-container").hide();
    return $(".register-container").hide();
  });
  appRouter.on("route:login", function() {
    var loginView;
    $(".login-container").show();
    $(".register-container").hide();
    return loginView = new LoginView({
      el: $(".login-container")
    });
  });
  appRouter.on("route:register", function() {
    var registerView;
    $(".register-container").show();
    $(".login-container").hide();
    return registerView = new RegisterView({
      el: $(".register-container")
    });
  });
  return Backbone.history.start();
});
